#include <bits/stdc++.h>using namespace std;void check(long long a,long long b,vector<long long>& arr) {                           long long cnt=b;         for(int i=0; i<arr.size(); i++) {         	cnt+= min(1+arr[i],a) -1;		 }	  	 cout<<cnt<<endl;      	}int main() {		int t;	cin>>t;	while(t--)  {				long long a,b,n;		cin>>a>>b>>n;		vector<long long> arr(n);		for(int i=0; i<n; i++) {			cin>>arr[i];		}		check(a,b,arr);	}	}