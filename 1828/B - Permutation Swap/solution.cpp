#include <bits/stdc++.h>#include <numeric>#include<cmath>using namespace std;void check(vector<int>& arr) {	  	 vector<int> diff;	 for(int i=0; i<arr.size(); i++) {	 	if(arr[i] != (i+1)) {	 		diff.push_back(abs(i+1-arr[i]));		 }	 }	 int res=0;	 for(int i=0; i<diff.size(); i++) {	 	  res=__gcd(res,diff[i]);	 }	 cout<<res<<endl;	   	}int main() {		int t;	cin>>t;	while(t--)  {			int n;	cin>>n;vector<int> arr(n);for(int i=0; i<arr.size(); i++) {	cin>>arr[i];}				check(arr);	}	} //1 3 4 2 5 6