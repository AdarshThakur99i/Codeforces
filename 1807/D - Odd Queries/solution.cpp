#include <bits/stdc++.h>#include <numeric>#include<cmath>using namespace std;//2 3 4 5 6 void check(vector<long long>& arr,int q) {	int n=arr.size();        vector<long long> pref(n,0);		vector<long long> suff(n,0);		for(int i=1; i<arr.size(); i++) {			pref[i]=pref[i-1]+arr[i-1];		}		for(int i=arr.size()-2; i>=0; i--) {			suff[i]=suff[i+1]+arr[i+1];		}		while(q--) {			  int l,r,k;			  cin>>l>>r>>k;			  long long sum=pref[l-1]+suff[r-1]+(l-r+1)*k;			  if(sum%2 ==0 ) {			  	cout<<"NO"<<endl;			  }			  else {			  	cout<<"YES"<<endl;			  }	  		}   		  	   	}int main() {		int t;	cin>>t;	while(t--)  {			int n,k;	cin>>n>>k;vector<long long> arr(n);for(int i=0; i<arr.size(); i++) {	cin>>arr[i];}				check(arr,k);	}	} //1 3 4 2 5 6